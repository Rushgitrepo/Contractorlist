/**
 * MOVED TO BACKUP
 * Original location: src/api/gc-apis/mock.ts
 * Reason: GC dashboard now uses real backend API; this mock is dev-only.
 */
/**
 * GC Dashboard API Endpoints
 * Mock API endpoints for development
 * Handles persistence to localStorage for testing purposes
 */

const STORAGE_KEYS = {
  PROJECTS: 'gc_mock_projects',
};
